apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reverse-ip.fullname" . }}-test-connection"
  labels:
    {{- include "reverse-ip.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reverse-ip.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
